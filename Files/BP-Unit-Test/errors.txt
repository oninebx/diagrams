  it.each`
    queryType
    ${'generalEnquiry'}
    ${'existingPolicy'}
    ${'missingPolicy'}
    ${'claim'}
    ${'quote'}
    ${'cancellation'}
  `(
    'should render default thank you message when submitting $queryType form for pacific brands',
    async ({ queryType }) => {
      process.env.brand = 'fij';
      renderComponent(<ContactUsThankYou />, { initialRoute: `/portal/contact/thankyou/${queryType}` });
      expect(screen.getByText('contactUs.thankYouPageTitle')).toBeInTheDocument();
      expect(screen.getByText('contactUs.submitConfirmationMessage')).toBeInTheDocument();
      expect(screen.getByRole('link', { name: 'Back to Overview' })).toBeInTheDocument();
    }
  );

ContactUsThankYou > should render default thank you message when submitting existingPolicy form for pacific brands
-----
TypeError: Invalid value used as weak map key